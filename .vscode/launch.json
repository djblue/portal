{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Portal",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": ["--extensionDevelopmentPath=${workspaceFolder}"]
        }
    ]
}
